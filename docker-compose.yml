version: '3'

services:
        jobs:
                build:
                        context: .
                        dockerfile: Dockerfile
                image: stevejobs
                container_name: jobs
                restart: unless-stopped
                command: tail -f /dev/null
